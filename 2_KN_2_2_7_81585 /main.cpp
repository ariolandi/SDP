#include<iostream>
#include"fileManager.h"
using namespace std;

int main()
{
  /*   std::ofstream outfile;
    outfile.open ("test.txt");
    Student st(81585, "Mariq", "Ilieva", 4);

    outfile<<st;
    outfile<<Student(81654, "Peter", "Ivanov", 5);
    long pos = outfile.tellp();
    outfile.seekp (sizeof(st));
    st = Student(81585, "Ivan", "Ganev", 4);
    outfile<<st;

    outfile.close();
*/
    CommandLine();

    return 0;
}
